<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="OpenTAKICU" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/appColor</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/appColor</item>
        <item name="android:colorBackground">@color/black</item>
        <!--<item name="android:buttonStyle">@style/buttonStyle</item>-->
        <!--<item name="buttonStyle">@style/buttonStyle</item>-->
        <!--<item name="colorOnSecondary">#FFFF0000</item>
        <item name="android:textColor">@color/WHITE</item>
        <item name="android:textColorSecondary">@color/GRAY</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>-->

    </style>

    <style name="buttonStyle"
        parent="android:style/Widget.Button">
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="sliderLabel" parent="@style/Widget.Material3.Tooltip">
        <item name="android:color">@color/appColor</item>

    </style>

    <style name="Theme.OpenTAKICU" parent="OpenTAKICU" />
</resources>